[phases.setup]
nixPkgs = [
  "chromium",
  "nss",
  "nspr",
  "atk",
  "at-spi2-atk",
  "xorg.libX11",
  "xorg.libXcomposite",
  "xorg.libXdamage",
  "xorg.libXext",
  "xorg.libXfixes",
  "xorg.libXrandr",
  "xorg.libxcb",
  "libxkbcommon",
  "mesa",
  "expat",
  "libdrm",
  "xorg.libXtst",
  "xorg.libXi",
  "alsa-lib",
  "pango",
  "cairo",
  "cups",
  "dbus",
  "gdk-pixbuf",
  "gtk3",
]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "playwright install chromium",
]

# NOTE: No [start] block here — Railway uses the Procfile instead.
# Having both causes a conflict; Procfile with gunicorn is the correct prod server.
